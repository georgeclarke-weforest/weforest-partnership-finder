import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Netlify's Next.js plugin handles SSR deployment automatically.
  // No output override needed — leave it as default (Node.js server).
};

export default nextConfig;
